//// This message will appear in the browser's developer console
//console.log("Bienvenue sur le site de Congo Developer ! Le JavaScript est prêt.");
//
//// Example of future interactivity:
//// You could add code here to handle clicks, load chat messages, etc.
//const navLinks = document.querySelectorAll('.main-nav a, .mobile-nav a');
//
//navLinks.forEach(link => {
//    link.addEventListener('click', (event) => {
//        event.preventDefault(); // Prevents the link from actually navigating
//        const linkText = link.textContent.trim().split('\n')[1] || link.textContent.trim();
//        alert(`Vous avez cliqué sur : ${linkText}`);
//    });
//});